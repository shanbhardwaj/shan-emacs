
# Org setup notes

How the org system is wired, the daily loop, and the decisions behind both —
including the ones that look arbitrary until you know what they were avoiding.
Config lives in `lisp/init-org.el`; the files themselves in `~/org`, synced
between the Mac and the 4090 by Syncthing (folder id `org`, sendreceive).

Two reference pages, kept current:
- Daily loop — https://claude.ai/code/artifact/7c15e135-1753-44c5-93c8-e4657aa6d42b
- Keys across both machines — https://claude.ai/code/artifact/8bbf0738-9214-4b5b-b84d-b371125fe99a

## Where things are

| File | Items | What it holds | Daily view? |
|---|---|---|---|
| `kulcare.org` | 7 | One project. Sections: Open questions / Tasks / Decisions / Notes | yes |
| `commander.org` | 4 | Same shape | yes |
| `finishd.org` | 4 | Same shape | yes |
| `trekka.org` | 5 | Same shape. Dormant, ready to restart | yes |
| `inbox.org` | 5 | Unsorted captures. Small on purpose | yes |
| `learning.org` | 16 | Emacs practice tasks, instructions in each body | **no** |
| `reading.org` | 11 | Saved links | **no** |
| `github.org` | 50 | Regenerated by `~/.local/bin/gh-issues-to-org`. Never hand-edit | own section |
| `worklog.org` | 42 | Writeups, via the `org-note` skill | — |
| `gcal.org` | 0 | Calendar sync target. Has never worked, see below | — |

`personal.org`, `projects.org`, `someday.org` are 0 bytes and unused. They have
been empty since Jul 28; the seed content is in
`~/Documents/orgenda-seed-backup/` if it is ever wanted. `projects.org` in
particular is *not* the project system — that is the four files above.

Counts above were accurate on 2026-08-28. `inbox.org.bak-20260827` holds the
pre-split inbox if anything looks lost.

## The two axes

An item belongs to a **project** and usually to a **person** — the person it
needs raising with. The people are a mix of dedicated and shared, so those axes
do not collapse into one, and a directory can only express one of them.

- Project is the **file**, because reading happens by sitting down on one project.
- Person is a **tag** (`:@ravi:`), because reviews are irregular and person-shaped.
- `#+FILETAGS` in each project file supplies the project tag automatically.

That last line is the trick that makes it cheap: the only tag ever typed is the
person, and one item then appears in both the project view and the person view
without being filed twice.

## The daily loop

Commitment is the tag `:today:`. Two short passes, one at each end of the day.

| Key | When | Does |
|---|---|---|
| `C-c o d` | morning | Planner — see below |
| `C-c o t` | any time | Commit the entry at point, or take it back. Works in a file and in the agenda |
| `C-c o r` | evening | Review what was committed |
| `C-c o D` | evening | Clear every `:today:` tag, so tomorrow starts empty |

### The morning pass

Open `C-c o d` and pick **three to five** things. Not a full triage — a
commitment. Everything not picked stays invisible until tomorrow, which is the
point: the value is in what gets left out.

The planner shows four blocks, in this order:

1. **Committed** — anything still tagged from yesterday. Usually empty. If it
   is not, decide deliberately whether it carries again rather than letting it
   drift.
2. **Today's calendar** — the agenda for the day. Empty until org-gcal works.
3. **Open questions** — `Q` and `ASKED` across every project. First because
   open questions are most of what gets captured, so this is usually the real
   pool.
4. **Everything else open** — `TODO` and `NEXT`, excluding `github`,
   `learning` and `reading`.

Press `C-c o t` on a line to commit it.

### During the day

`C-c o a d` is the dashboard. It leads with **Today**, then calendar, in
progress, undated inbox, GitHub, and waiting-on-someone. Capture keeps working
as always — new things land in their project file and wait for tomorrow rather
than interrupting today.

### The evening pass

`C-c o r` shows one block: what you committed to. Mark what got done. Then
`C-c o D` wipes every tag.

Deliberately **not** part of the evening: choosing tomorrow's list. Clear and
carry only. Selection happens in the morning with fresh eyes, and an evening
pass that also demands decisions is the one that gets skipped on a tired night.

**The clear is the load-bearing step.** A loop that never empties is just a list
that grows, which is the failure this whole design exists to avoid. Carrying
something over should be a deliberate act tomorrow, not a default.

### Why a tag and not a date

A tag sits outside the state machine, which matters because open questions are
most of what gets captured and a `Q` and a `TODO` can both be today's work.
`SCHEDULED` means *start on this date*, so an unfinished day becomes a growing
overdue list — that guilt is what kills these systems. `NEXT` keeps meaning
actively in progress.

`shan/org-today-clear` only removes **local** tags (`org-get-tags nil t`), never
inherited ones, so `#+FILETAGS` is never damaged. Verified end to end.

### Reviews are a query, not a calendar

Meetings with the teams are irregular, so nothing recurring gets scheduled. The
useful question is never "what is on the calendar" but "I am about to talk to
someone — what do I have for them". That is `C-c o w`: prompts for a person tag
and shows everything open for them across all four projects. It is the reason
the person is a tag rather than a file.

### Why the inbox became five files

One inbox was holding five different kinds of thing, and mixing them meant the
daily view showed 34 saved browser tabs every morning. Split by what each item
actually is:

- **project work** → the four project files
- **Emacs practice** (16, with instructions in each body — a curriculum) →
  `learning.org`, excluded from daily views
- **saved links** (11) → `reading.org`, excluded from daily views
- **genuine loose captures** (5) → `inbox.org` stays small on purpose
- **GitHub issues** → generated, its own dashboard section

Each file now means one thing, and the inbox means "not yet sorted" again.

## Other keys

| Key | Does |
|---|---|
| `C-c o a d` | Dashboard — Today, calendar, in progress, inbox, GitHub, waiting |
| `C-c o p` | Open a project file |
| `C-c o w` | Everything open for one person, across all projects |
| `C-c o c` | Capture |
| `C-c o i` / `C-c o n` | Open inbox / notes |
| `C-c o G` | Regenerate `github.org` |
| `C-c o g` | org-gcal sync (does not work yet) |

## Capture

Letters are the same on all three surfaces: `q` question, `t` task, `n` note.
Emacs and Leader Key namespace the project ones under `p`; the 4090 has no
leader concept, so **Shift means project** there.

| Captures | Emacs | Mac | 4090 |
|---|---|---|---|
| Project question | `C-c o c` `p q` | `⌘ k p q` | `⌥⌃⇧Q` |
| Project task | `C-c o c` `p t` | `⌘ k p t` | `⌥⌃⇧T` |
| Project note | `C-c o c` `p n` | `⌘ k p n` | `⌥⌃⇧N` |
| Inbox task | `C-c o c` `t` | `⌘ k t` | `⌥⌃T` |
| Note | `C-c o c` `n` | `⌘ k n` | `⌥⌃N` |
| Web link | `C-c o c` `w` | `⌘ k w` | `⌥⌃W` |

Project captures prompt for the project, then for tags. Web link is on `w` and
not `l` because `Ctrl+Alt+L` is the 4090's lock screen.

Scripts: `dotfiles/bin/org-capture-safari` (Mac) and
`dotfiles/linux/bin/org-capture-linux` (4090). The Linux one has a `case`
listing the templates it handles — **add new template keys there** or they fall
through to the web-link path and try to scrape the active window title.

## Keywords

```
TODO(t) NEXT(n) WAITING(w) | DONE(d) CANCELLED(c)
Q(q) ASKED(a)              | ANSWERED(A) MOOT(m)
```

Questions have their own sequence because a question is raised, waits, then is
answered — it is not work that stalls. When an answer is worth keeping, move the
item to `* Decisions` in the same project file. Most just close.

## Open items

**The calendar has never worked.** `gcal.org` is empty and `~/.authinfo` has no
`org-gcal` entry at all — `auth-source-pick-first-password :host "org-gcal"`
returns nil. This is not a sync that broke; it never authenticated. Needs Google
Cloud OAuth credentials (enable Calendar API, create an OAuth client), then:

```
machine org-gcal login client-id password <id>
machine org-gcal login client-secret password <secret>
```

The planner has a calendar block already waiting for it.

**Half of capture never reaches org.** Away from both machines, things go to a
Slack message to self. Nothing moves those into org. This is the largest
structural gap — the loop will work for desk capture and be blind to the rest.
Options discussed: an on-demand pull, a sweep habit, or a background sync. No
decision taken.

**50 GitHub issues are genuinely open.** `github.org` was refreshed 2026-08-28
and still returns 50 (38 in `kulcare-platform`). The script fetches only
`--state open --assignee @me`, so if the work is done, the issues need closing
on GitHub. The org file is accurate; GitHub is what is stale.

**The habit is untested.** Zero items have ever moved to DONE in any file. The
machinery is built and verified; the daily practice has not started. Treat
usage-based conclusions with suspicion until there is real history.

**Two org copies exist.** `~/Library/Mobile Documents/iCloud~com~yakshaven~orgenda/`
holds a second, diverging set from the Orgenda iOS app, stale since Aug 19-20.
Not currently conflicting with Syncthing. No decision on retiring it.

## Gotchas

- **Moving a level-1 heading under a section makes it a sibling, not a child.**
  Demote it. This bit the kulcare refile; caught by `org-map-entries` showing
  `L1` where `L2` was expected.
- `org-agenda-custom-commands` lives in `org-agenda`, not `org` — a batch test
  that only requires `org` fails with a void variable.
- `emacs --batch --eval` quoting is unreliable through ssh and heredocs. Write a
  `.el` file and use `-l` instead; several checks silently produced nothing
  before this was noticed.
- `use-package :bind` warns "defined multiple times" for every function named in
  `:bind` and defined later in the same file. Pre-existing and harmless.
- The `org-note` skill writes to `~/org/worklog.org`. Never hardcode the path;
  `~/org` is a Syncthing folder.
